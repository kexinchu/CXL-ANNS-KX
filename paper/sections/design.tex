% =============================================================================
% §4 Design  — Route A: all five modules. Target: 2.5--2.8 pages.
% 4.5/5 bar: each module names the finding it answers; interfaces are concrete;
% what is forbidden is as clear as what is done.
% 填写: 换 Fig.~6--8；补 API 名字与真实阈值；不要新增第六个“也做了”的模块。
% =============================================================================
\section{Design}
\label{sec:design}

\sys{} is a userspace ANNS runtime plus a thin residency plane in front of
host DRAM, an optional CXL-DRAM window, and a flash-backed CXL address
space (Figure~\ref{fig:design-arch}). Search is still best-first graph
walk. What changes is \emph{where} each object lives, \emph{which} cold
records are made resident, and \emph{when} a miss is issued relative to
other work.

\begin{figure*}[t]
  \centering
  \phbox{0.98\textwidth}{3.8cm}{%
    Fig.~6 placeholder --- architecture:\\
    top: Placement $|$ Promote Ctrl $|$ Shared Hot Set $|$ Search Ctrl;\\
    middle: residency plane (cache / uncache / promote / stats);\\
    bottom: host DRAM $|$ CXL-DRAM window $|$ CXL-SSD corpus.}
  \caption{\sys{} architecture. The residency plane is the only path that
    moves full-precision bytes between CXL-SSD and the hot window. Search
    never page-faults a neighbor ``to be helpful.''
    \textbf{Fill-in:} use the implemented component names; mark the
    software-proxy device explicitly.}
  \Description{Layered architecture of the ANNS runtime over three memory
    tiers.}
  \label{fig:design-arch}
\end{figure*}

\subsection{D1: Semantic placement}
\label{sec:d1}

Table~\ref{tab:placement} is the default map. The graph adjacency and the
PQ codebook are touched on every hop and are small relative to the
full-precision corpus; they live in host DRAM so they never compete with
vectors for the CXL-SSD window (F2, F7). Query state (candidates, visited
set, thread-local arenas) is likewise host-local. Full-precision vectors
default to the CXL-SSD address space. A bounded \emph{hub / shared hot
set} of records is explicitly cached into the window or into CXL-DRAM
(\S\ref{sec:d3}). Frontier-top neighbors are candidates for precise
promotion, not for bulk 2-hop prefetch (F3).

This is a deliberate difference from DiskANN, where adjacency and vectors
share SSD sectors. We pay a modest host-DRAM tax for the graph in order
to stop the window from turning into an accidental adjacency cache.

\begin{table}[t]
  \caption{Default placement. \textbf{Fill-in:} bytes on the corpus you
    report (e.g., 25M $\times$ dim).}
  \label{tab:placement}
  \small
  \begin{tabular}{@{}lll@{}}
    \toprule
    Object & Default tier & Why \\
    \midrule
    Graph ($R$ neighbors) & host DRAM & every hop; keep off the window \\
    PQ / codebook        & host DRAM & tiny, extremely hot \\
    Query / visited      & host DRAM & per-worker state \\
    Full-precision vec.  & CXL-SSD   & capacity \\
    Hub / shared hot set & window / CXL-DRAM & F7 \\
    Frontier-top nbrs    & bounded promote & F3, F5 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{D2: Bounded precise promotion}
\label{sec:d2}

Promotion is an explicit \texttt{promote(ids[], level, budget)} on the
residency plane (Figure~\ref{fig:design-promote}). The only ids that
enter the call are the neighbors of the \emph{current frontier top-$M$}
--- the nodes the walk will expand next. A global and a per-query byte
budget cap how much may move; overflow falls back to demand fill. The
target level is a choice (F1): install into the device/software cache
only, or also install PTEs. The runtime tracks
$\mathrm{precision}=\mathrm{used}/\mathrm{promoted}$ online and shrinks
$M$ when precision falls below a threshold.

We forbid the policies F3 already falsified: synchronous high-coverage
2-hop prefetch, and ``fault harder'' loops that raise queue depth by
touching unused pages. Over-promotion is the failure mode the byte
budget exists to prevent.

\begin{figure}[t]
  \centering
  \phbox{0.98\columnwidth}{3.4cm}{%
    Fig.~7 placeholder --- promote path:\\
    frontier top-$M$ $\rightarrow$ budget check $\rightarrow$ async fill
    $\rightarrow$ window insert $\rightarrow$ precision feedback.}
  \caption{Bounded precise promotion. Only soon-to-expand neighbors are
    eligible; a byte budget and a live precision estimate keep the window
    from becoming a junkyard.
    \textbf{Fill-in:} annotate the implemented ioctl / batch path and the
    default $M$ / budget used in~\S\ref{sec:eval}.}
  \Description{Flow diagram of bounded promotion with a precision feedback
    loop.}
  \label{fig:design-promote}
\end{figure}

On corpora where several vectors share a 4\,KB page (low dimension), a
\emph{page-bin / reorder} layout clusters IDs that the walk co-touches so
that one fill admits several useful records. This is layout serving
residency, not a new index family. We report it as a placement option
and show when it helps (small dim) and when it does not (vector $\approx$
page).

\subsection{D3: Shared hot set}
\label{sec:d3}

Single-query deepening is a bad way to spend a small window (F4, F7).
\sys{} maintains a cross-query hot set: a fixed byte budget (a configured
fraction of the CXL-SSD DRAM window) holding either high in-degree hubs
or IDs that a short sliding window of queries repeats. Replacement is
LFU with aging so a burst cannot freeze the set. The hot set is
\emph{decoupled} from beam: the default is a modest beam plus a warm
shared set, not a large beam on a cold device.

The API is \texttt{cache(ids[])} / \texttt{uncache(ids[])}. Pinning is
never implicit in a page fault. Multi-tenant isolation, if enabled,
partitions the budget; the paper evaluation uses a single tenant unless
stated.

\subsection{D4: Stall-aware search and continuous batching}
\label{sec:d4}

The search controller maximizes QPS at a recall@$k$ floor (or maximizes
recall at a fixed time/IO budget). It is \emph{not} a one-shot ``if miss
rate is high, cut beam'' rule. Offline, we record beam $\rightarrow$
(recall, IO) curves. Online, the controller picks the smallest beam that
meets the recall floor, or stops when marginal recall per additional IO
falls below a threshold. When D3 reports a high hot-set hit rate the
controller may raise beam slightly, because the extra candidates are
cheap.

Continuous fetch-level batching is the mechanism that makes that control
loop feasible on a slow miss path (Figure~\ref{fig:design-pipe}). A
global scheduler keeps a target number $D$ of memory-semantic fills
in flight, admitting the next read when one retires --- not when a
query, a hop, or a static group finishes. Fills are issued with an
asynchronous batch interface so that $D$ concurrent NAND operations
actually reach the device. Scoring of already-resident records proceeds
on the host and is never queued behind a fill it does not depend on.

\begin{figure*}[t]
  \centering
  \phbox{0.98\textwidth}{3.4cm}{%
    Fig.~8 placeholder --- miss pipeline:\\
    queries $\rightarrow$ fetch-level scheduler (target $D$) $\rightarrow$
    async CXL-SSD fills $\rightarrow$ host score $\rightarrow$ admit next.\\
    Show why blocking load/store cannot hold $D$.}
  \caption{Continuous fetch-level batching over asynchronous
    memory-semantic fills. Recall is unchanged by construction: prefetch
    only changes \emph{when} a needed page is resident, not \emph{which}
    nodes the walk visits.
    \textbf{Fill-in:} draw the implemented \texttt{cont} path; put the
    blocking-mmap failure mode on the same figure.}
  \Description{Scheduler diagram that holds a target device queue depth
    with async fills.}
  \label{fig:design-pipe}
\end{figure*}

\subsection{D5: Observability}
\label{sec:d5}

Every experiment in~\S\ref{sec:eval} is required to report the same
counters the controller sees: $\Delta$NAND reads, $\Delta$faults,
soft-cache / PTE / path hits, promote precision, hot-set hit rate,
in-flight depth, and iso-recall@$k$. QPS without those counters is not a
result. The residency plane exports \texttt{stats()} for this purpose.

\paragraph{What we refuse to hide.}
If a module cannot move the iso-recall curve in an ablation, it is
removed from the contribution list rather than kept as decoration. That
is the bar for~\S\ref{sec:eval-ablate}.
