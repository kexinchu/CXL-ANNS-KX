% =============================================================================
% §3 Motivation  — Target: 1.4--1.6 pages. Intro stated C1--C3; this section
% measures them. Every finding is (A) exists (B) hurts. No hide QPS, no Design
% preview, no F7/LFU module. Figures: captions are the contract; fill bodies
% later. Corpus on current bars is LAION-200k (see captions).
% =============================================================================
\section{Motivation}
\label{sec:motivation}

Section~\ref{sec:background} defined the walk and the fill.
This section measures what happens if that fill is treated as a slightly
slow DRAM load or as a DiskANN sector read.
Each measurement answers two questions: does the effect exist, and how
much does the default cost?
The three results are the constraints Intro named C1--C3
(Table~\ref{tab:findings}).

\subsection{The miss cliff}
\label{sec:mot-cliff}

A host \texttt{load} of one vector from CXL-DRAM is a few hundred
nanoseconds.
The same load after a CXL-SSD window miss is a NAND fill
(Figure~\ref{fig:mot-cliff}).
On Text2Image-10M ($800$\,B) the $p_{50}$ is $0.28\,\mu$s versus
$89\,\mu$s ($\sim$320$\times$).
On a 10M-item LAION slice ($2048$\,B) it is $0.67\,\mu$s versus
$89\,\mu$s ($\sim$130$\times$).
The miss is a 4\,KB fill, so record size barely moves it; the
CXL-DRAM hit scales with bytes.

Graph search multiplies the tax by hops $\times$ degree.
Once a modest fraction of scores miss, wall-clock time is in the NAND
path, not in distance arithmetic.
DiskANN already knew the cliff for block I/O.
The new fact is that a memory-semantic interface does not remove it:
\texttt{load}/\texttt{store} only changes \emph{how} a miss is issued
(\S\ref{sec:mot-qd}).
Treating CXL-SSD as mmap'd DRAM therefore makes search miss-bound.
The runtime contract that follows is not ``make NAND faster''; it is
that the scoring instruction must not wait on a fill.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.98\columnwidth]{figs/mot-cliff.pdf}
  \caption{One-item host load: CXL-DRAM hit versus CXL-SSD cache miss.
    CXL-DRAM is Montage \texttt{/dev/dax0.0} (\texttt{clflush} then load).
    CXL-SSD is a \texttt{vmem\_sw} window miss.
    T2I-10M uses the staged 800\,B vectors.
    LAION-10M is the first $10$M items of the 25M dump ($d{=}512$);
    the miss bar matches that item size on the same SSD path.}
  \Description{Grouped bars of one-item latency on two corpora,
    CXL-DRAM versus CXL-SSD miss, log scale.}
  \label{fig:mot-cliff}
\end{figure}

\subsection{Prefetch must be wise}
\label{sec:mot-page}

Neighbor IDs are not sequential in the address space, so the next
vector page is not the next byte.
Admitting 16\,KB pages (or cachelines grouped as pages) therefore
brings siblings the walk will not score.
On a high-dimensional corpus where a record already fills most of a
page, per-record admission versus page admission splits QPS by
$\sim$3.6$\times$ and NAND reads by $\sim$3.7$\times$
(Figure~\ref{fig:mot-pathology}a).
At small dimensions several records share a page and the gap
narrows; page-cache thinking is still the wrong default here.

\label{sec:mot-pref}
Synchronous logical prefetch of a 1-, 2-, or 8-hop neighborhood does
not fix the cliff.
Hit rate is almost unchanged, NAND reads multiply, and QPS drops by
$\sim$4--5$\times$ at the widest setting
(Figure~\ref{fig:mot-pathology}b).
Precision (\emph{used$/$promoted}) falls as coverage grows; leftover
pages evict useful ones from a small window.
Wrong pages cost more than a missed page.
The useful policy is a bounded ensure of the nodes the search is
about to expand---not ``prefetch harder.''

\label{sec:mot-beam}
Widening the beam is the same mistake with a quality knob.
Raising $L$ grows the unique IDs touched per query, cools the
window, and raises NAND reads faster than it raises recall
(Figure~\ref{fig:mot-pathology}c).
Recall saturates once a hop budget is spent; further width is only
stall.
A controller that chops $L$ on instantaneous miss rate does not, by
itself, win at iso-recall: it has no recall constraint and no IO
budget.
Quality and stall must be read together.
That is a measurement discipline, not a third design module.

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.98\textwidth]{figs/mot-pathology.pdf}
  \caption{Unwise admission amplifies the cliff
    (LAION-200k, $d{=}1024$, hops${=}32$).
    (a)~Page vs.\ record.
    (b)~Blind synchronous graph prefetch.
    (c)~Widening $L$ (coverage as another form of unwise).
    Absolute recall in (c) is low because of the hop cap; the
    32$\rightarrow$64 plateau is the claim.}
  \Description{Three-panel figure of page admission, prefetch
    precision, and search-width coupling.}
  \label{fig:mot-pathology}
\end{figure*}

\subsection{One query cannot fill the device}
\label{sec:mot-qd}

Best-first search is pointer-chasing: hop $i$ must score before the
walk knows which vector pages hop $i{+}1$ needs.
A single query therefore leaves device-queue bubbles---the beam
ramps slowly, the next fill is unknown, and the walk drains at the
end (Figure~\ref{fig:mot-bubbles}).
On a block device, interleaving independent queries raises
occupancy because the first-class object is already an I/O queue.
On CXL-SSD the programming model is a faulting address space.
A blocking \texttt{load}/\texttt{store} (or a blocking
\texttt{memcpy} from the mapping) is a synchronous NAND fault:
effective queue depth collapses to $\approx 1$ per thread.
Issuing speculative fills to keep the device busy, then scoring
those pages on the critical path, pays the cliff again.

The idle device is a pipeline problem, not a bandwidth wall.
Continuous issue across queries is what can hold depth; it does
not, by itself, make one walk as fast as an in-window oracle, and
it does not change which nodes the walk visits.
The implementation of that issue belongs in~\S\ref{sec:design}.

\begin{figure}[t]
  \centering
  \input{sections/fig_mot_bubbles}
  \caption{Pipeline bubbles on a faulting CXL address space
    (schematic).
    $y$ is in-flight fills; $D$ is the depth NAND parallelism needs.
    A single walk cannot hold $D$; a blocking load/store is stuck at
    QD$\approx 1$; a static batch drains at the barrier.
    How the runtime issues across queries is~\S\ref{sec:design}.}
  \Description{Three timelines of device queue occupancy under
    naive single-query and static-batch issue.}
  \label{fig:mot-bubbles}
\end{figure}

\subsection{From measurements to constraints}
\label{sec:mot-map}

Table~\ref{tab:findings} is the contract for~\S\ref{sec:design}.
We do not treat CXL-SSD as uniform memory (C1): scoring must not
touch NAND.
Prefetch must be wise (C2): only the soon-to-expand frontier, packed
so a fill carries co-scored neighbors, under a byte budget.
One walk cannot fill the device (C3): issue must be asynchronous and
cross-query so pointer-chasing bubbles do not collapse queue
depth.\footnote{A small shared pin of entry vertices helps a narrow
beam more than deepening one walk. That is an admission hint, not a
positive LFU-hot-set module, and not a fourth challenge.}
Placement of neighbor IDs in host DRAM is a premise for C2, not a
separate challenge: without it the prefetcher misses once just to
learn whom to fetch.

\begin{table}[t]
  \caption{Measured pathologies become the three constraints
    Intro named.
    Deltas are from LAION-200k on the software CXL path; the
    qualitative claims are what Design must answer.}
  \label{tab:findings}
  \small
  \begin{tabular}{@{}p{0.08\columnwidth}p{0.34\columnwidth}p{0.24\columnwidth}p{0.22\columnwidth}@{}}
    \toprule
    ID & Exists $+$ hurts & Constraint & \sys{} answers \\
    \midrule
    C1 & Miss cliff $\sim$130--320$\times$ & Scoring must not wait on NAND & Score-hide contract \\
    C2 & Page $\sim$3.6$\times$; blind prefetch $\sim$4--5$\times$ & Prefetch only the expand frontier & Wise prefetcher \\
    C3 & Pointer-chasing; blocking fill $\Rightarrow$ QD$\approx 1$ & Hold depth across walks & Continuous batching \\
    \bottomrule
  \end{tabular}
\end{table}
